# AI Swarm

A parallel, stack-agnostic multi-agent swarm orchestration platform.

## Quick Start

```bash
# Install
pip install -e ".[dev]"

# Validate a config
swarm validate example_config.yaml

# Run a swarm
swarm run example_config.yaml
```
